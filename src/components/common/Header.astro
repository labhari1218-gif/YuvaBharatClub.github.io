---
import AnimatedLogo from "./AnimatedLogo.astro";
import MobileNav from "./MobileNav.astro";
import { isCurrentPath, navLinks, withBase } from "../../lib/site";

interface Props {
  currentPath: string;
  siteTitle: string;
  shortTitle: string;
}

const { currentPath, siteTitle, shortTitle } = Astro.props;
---

<header class="site-header">
  <div class="container site-header__inner">
    <a class="brandmark" href={withBase("/")}>
      <AnimatedLogo size={56} label={`${siteTitle} logo`} variant="soft" />
      <span>
        <strong>{shortTitle}</strong>
        <small>{siteTitle}</small>
      </span>
    </a>

    <nav class="desktop-nav" aria-label="Primary">
      <ul>
        {
          navLinks.map((link) => (
            <li>
              <a
                href={withBase(link.href)}
                aria-current={isCurrentPath(currentPath, link.href) ? "page" : undefined}
              >
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>

    <MobileNav currentPath={currentPath} />
  </div>
</header>
